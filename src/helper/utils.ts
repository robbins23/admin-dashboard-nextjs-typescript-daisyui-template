// utils.ts

/**
 * Converts meters to kilometers.
 * @param meters The value in meters to be converted to kilometers.
 * @returns The value converted to kilometers.
 */
export function metersToKilometers(meters: number): number {
    return meters / 1000;
}
